# ai-multi-agent-document-reviewer
A multi-agent AI system that ingests PDF/DOCX reports, performs semantic chunking, context-aware retrieval, and generates four types of review comments—recommendations, evidence, clarifications, and proofreading. Built with LangGraph, vector search, delta embeddings, and configurable YAML rules.
